#pragma once

#include <string>
#include <optional>

namespace TB_NS {
    using Str = std::string;
    using StrOpt = std::optional<Str>;

    using Int8Opt = std::optional<int8_t>;
    using Int8Opt = std::optional<int8_t>;
} // namespace TB_NS
